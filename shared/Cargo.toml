[package]
name = "ufw-shared"
description = "Wire protocol, policy, identity and log types shared by every Unified Firewall component"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
build = "build.rs"

[lib]
name = "ufw_shared"
path = "src/lib.rs"

[dependencies]
